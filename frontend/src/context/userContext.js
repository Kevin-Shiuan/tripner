import { createContext } from "react";

const userContext = createContext()

const passwordContext = createContext()

const signInContext = createContext()


export {userContext, passwordContext, signInContext}